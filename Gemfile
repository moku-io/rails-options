source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in rails-options_config.gemspec.
gemspec

gem 'rubocop'

gem 'puma'

gem 'sqlite3'

gem 'sprockets-rails'
